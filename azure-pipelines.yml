# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none

pr:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    echo "$SYSTEM_PULLREQUEST_SOURCEREPOSITORYURI"
    echo "$SYSTEM_PULLREQUEST_SOURCEREPOSITORYURI" | cut -d / -f 4
    curl -H "Authorization: token $(GitHubPAT)" "https://api.github.com/teams/1649488/memberships/mdkinney" | python -c "import sys, json; print(json.load(sys.stdin)['state'])"
  displayName: 'Run a multi-line script'

# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none

pr:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    curl -H "Authorization: token $(GitHubPAT)" -s "https://api.github.com/teams/1649488/memberships/A`echo "$SYSTEM_PULLREQUEST_SOURCEREPOSITORYURI" | cut -d / -f 4`" | python -c "import sys, json; print(json.load(sys.stdin)['state'])"
  displayName: 'Run a multi-line script'
